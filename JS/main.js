// if we want to add a `$` sign in front of variables
// then we have to do the following
// `You bought ${quantity} ${items}, total price: $${price * quantity}`;
// Here, the first `$` sign works as a string and second `$` sign works
// as the part of the String literate template 


alert('its working.');